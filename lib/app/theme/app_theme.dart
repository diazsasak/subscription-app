import 'package:flutter/material.dart';

final mainColor = Colors.blueAccent;

final theme = ThemeData(
  appBarTheme: AppBarTheme(
    elevation: 0,
    centerTitle: true,
    color: mainColor,
  ),
  primaryColor: mainColor,
);
